// עולמות התוכן של האתר — הקטגוריה הראשית של כל סיפור, אחת בדיוק לכל סיפור.
// החלוקה נגזרה מהתוכן עצמו (משדה המקור שבכרטיס הסיפור) ולא מתיאוריה:
// מי זה קרה לו? לאף אחד → משל · לדמות מוכרת → צדיקים · לי או למי שפגשתי → יומן.
//
// slug באנגלית לכתובות נקיות; התווית והשורה בעברית לתצוגה.
// accent — צבע מותג אחד לכל עולם, מופיע כקו דק בדלת ובתווית שבכרטיס.
// linkText — נוסח הקישור אל העולם ("כל המשלים"). חי כאן ולא בעמוד מסוים,
// כי גם דף הבית וגם עמוד הסיפורים צריכים אותו.
// hidden — עולם ששמור לעתיד ואין בו עדיין תוכן: אינו מוצג כדלת בעמוד הסיפורים
// (דלת ריקה נראית כתקלה), אבל קיים בסכמה ונפתח מעצמו ברגע שיהיו בו סיפורים.
//
// **השורה מסבירה ולא רומזת.** הנוסח הקודם היה פיוטי ("סיפור שלא קרה — כדי
// שתראה בו את מה שכן") ויפה כשלעצמו, אבל הוא תיאר את *הרעיון* של העולם ולא
// את מה שהקורא ימצא בו. השורה היא גם תיאור המטא של עמוד העולם בגוגל, ולכן
// משפט שמסביר עובד שם טוב יותר ממשפט שרומז.
export interface Section {
  label: string;
  line: string;
  linkText: string;
  accent: string;
  hidden?: true;
}

export const SECTIONS_META = {
  meshalim: {
    label: 'משלים',
    line: 'מלך, גשר, נשר. אף אחד מהם לא היה — ודווקא לכן קל יותר לראות בהם את עצמך.',
    linkText: 'כל המשלים',
    accent: 'var(--brick)',
  },
  tzadikim: {
    label: 'סיפורי צדיקים',
    line: 'לא ניסים ולא מופתים. רגע אחד קטן שבו אדם גדול עשה מה שעשה — ובו הכול.',
    linkText: 'כל סיפורי הצדיקים',
    accent: 'var(--teal)',
  },
  yoman: {
    label: 'מהיומן שלי',
    line: 'לא סיפורים ששמעתי. דברים שהייתי שם כשהם קרו — כולל אלה שלא יצאתי מהם יפה.',
    linkText: 'הכל מהיומן',
    accent: 'var(--blue)',
  },
} as const satisfies Record<string, Section>;

export type SectionSlug = keyof typeof SECTIONS_META;

export const SECTION_SLUGS = Object.keys(SECTIONS_META) as SectionSlug[];

/** התוויות בלבד — לשימושים שצריכים רק את השם (כרטיס, כותרת עמוד). */
export const SECTIONS = Object.fromEntries(
  SECTION_SLUGS.map((s) => [s, SECTIONS_META[s].label])
) as Record<SectionSlug, string>;
