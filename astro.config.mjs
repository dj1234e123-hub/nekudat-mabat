// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // הכתובת הזמנית — לעדכן לדומיין הקבוע בשלב 6 (חיבור הדומיין).
  // משמשת לקישורים מוחלטים: שיתוף, תגיות תצוגה מקדימה (Open Graph).
  site: 'https://nekudat-mabat-site.vercel.app',
});
